
  # VentureLab Prototype Structure

  This is a code bundle for VentureLab Prototype Structure. The original project is available at https://www.figma.com/design/wvP82zZXYA6hmfIif515u9/VentureLab-Prototype-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  