fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
httpx==0.27.2
pytest==8.3.3
pytest-cov==5.0.0
faker==37.0.0
SQLAlchemy==2.0.36
alembic==1.13.3
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt<4
cryptography>=42

# Database drivers
# Postgres (Supabase / Vercel deploy) — used when DATABASE_URL starts with postgresql://
psycopg2-binary==2.9.10
# SQL Server (local dev only) — uncomment if pointing DATABASE_URL at a local
# SQL Server Express instance. Not installable in the Vercel build sandbox.
# pyodbc==5.1.0
